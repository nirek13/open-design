{
  "//": "Edge functions import the hosting core straight from packages/hosting/src as TypeScript. That is deliberate: the daemon runs the bundled dist of the same source, so path resolution, slug rules, and the manifest format cannot drift between a local preview and the deployed site.",
  "imports": {
    "@supabase/supabase-js": "jsr:@supabase/supabase-js@2"
  },
  "compilerOptions": {
    "strict": true,
    "lib": ["deno.window", "dom"]
  },
  "fmt": {
    "lineWidth": 100,
    "singleQuote": true
  }
}
